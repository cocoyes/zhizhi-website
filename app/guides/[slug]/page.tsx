import Link from"next/link";import{notFound}from"next/navigation";import type{Metadata}from"next";import{guides,getGuide}from"../data";export function generateStaticParams(){return guides.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,p=getGuide(slug);return p?{title:p.title,description:p.description,alternates:{canonical:`/guides/${slug}`},openGraph:{type:"article",title:p.title,description:p.description}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,p=getGuide(slug);if(!p)notFound();const related=guides.filter(x=>x.slug!==slug).slice(0,3);return <main className="article"><nav><Link href="/" className="brand"><i>枝</i>知枝</Link><Link href="/guides">全部指南</Link></nav><article><header><p className="eyebrow">— {p.category}</p><h1>{p.title}</h1><p className="intro">{p.intro}</p><div className="meta">知枝编辑部　·　更新于 2026 年 8 月　·　{p.read}</div></header><div className="toc"><b>本文要点</b>{p.sections.map((x,i)=><a href={`#section-${i}`} key={x.title}>{i+1}. {x.title}</a>)}</div>{p.sections.map((x,i)=><section id={`section-${i}`} key={x.title}><h2>{x.title}</h2><p>{x.body}</p>{x.points&&<ul>{x.points.map(v=><li key={v}>{v}</li>)}</ul>}</section>)}<aside><b>少记一点琐事，多留一点从容。</b><p>知枝可以帮你记录、提醒、整理与思考。</p><Link href="/download">了解知枝 App　→</Link></aside></article><section className="related"><p className="eyebrow">— 延伸阅读</p><h2>继续了解 AI 与生活</h2><div>{related.map(x=><Link href={`/guides/${x.slug}`} key={x.slug}><small>{x.category}</small><h3>{x.title}</h3><b>阅读 →</b></Link>)}</div></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"Article",headline:p.title,description:p.description,datePublished:"2026-08-13",dateModified:"2026-08-13",author:{"@type":"Organization",name:"知枝"},publisher:{"@type":"Organization",name:"知枝"},mainEntityOfPage:`https://zhizhi.deepyou.top/guides/${slug}`})}}/></main>}
